/**
 * @index => Start
 * @show => Show single item
 * @create => Create new item / items
 * @update => Update item / items
 * @destroy => Delete item / items
 */
exports.index = (req, res) => {
  res.render("index");
};
